The helper scripts now live in `tools/`. Run them via `bash tools/<script>`
or use the compatibility wrappers kept in this directory:

- `./scripts/setup.sh` → `./tools/setup.sh`
- `./scripts/migrate.sh` → `./tools/migrate.sh`
- `./scripts/run.sh` → `./tools/run.sh`
